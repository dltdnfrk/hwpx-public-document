[project]
name = "public-document-core"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["cryptography>=41", "pillow>=10"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
