# Canonical Projects

| Project | Canonical root | Public remote | Status |
| --- | --- | --- | --- |
| `hwpx-public-document` | `/Users/hyunjun/Documents/MUNI/hwpx-public-document` | `origin` registered, no push | private implementation, no push |

The implementation worktree is derived from this canonical root. A release must be promoted back to the canonical root and pass the Evaluation Protocol v1 before any public remote is configured.
